\documentclass{article}
% Here, "geometry" package is used for setting up the page layout
% In the optional command, we use, a4 paper for page layout
% and landscape as page orientation
\usepackage[a4 paper, landscape]{geometry}

\begin{document}
% To get a horizontal line from the point where text starts
% For this reason. "textwidth" command is used.
% Here, 1pt is the thickness of the horizontal bar.
\noindent\rule{\textwidth}{1pt}
    This is the A4 paper layout with the help of geometry package.
\end{document}
