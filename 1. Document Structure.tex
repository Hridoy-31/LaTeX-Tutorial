%\command_name[Optional_argument]{Compulsory_argument}
% Here, "documentclass" is the command_name, "article" is the compulsory argument
\documentclass{article}
% To enter into an environment, \begin and \end is used.
% Here, "document" is an environment
\begin{document}
    first document.\\   % Double slashes creates a new line.
    second sentence.
\end{document}
